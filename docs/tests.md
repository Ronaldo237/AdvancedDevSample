
# Tests unitaires et intégration

Le projet contient :

- **Tests Domain** : vérifie les entités (`ProductTest`, `OrderTest`)
- **Tests Application** : services (`ProductServiceTests`, `OrderServiceTests`)
- **Tests API Integration** : endpoints sécurisés et CRUD

### Commande pour exécuter les tests

```bash
dotnet test
